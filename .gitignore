# Runtime state — regenerated by dagu, never committed.
data/
logs/
suspend/
dags/docs/
dags/.dag.index

# Dagu writes these on first run in a DAGU_HOME it manages itself.
base.yaml
.base-config-created
.examples-created

.env
.DS_Store
